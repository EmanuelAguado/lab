# Base App

Base App will be a template repository for desktop applications built with **PySide6**.

It is intended to provide a basic application setup, a splash screen implementation, and a simple project structure that can be used as a reference for organizing application code.

The repository will include minimal examples of application initialization, user interface components, and resource management.